import React from 'react';
import PropTypes from 'prop-types';

const ConnectedHeader = () => (
  <header>

  </header>
);

ConnectedHeader.propTypes = {};

ConnectedHeader.defaultProps = ({});

export default ConnectedHeader;
